# AnNam Spa Static Theme

Static recreation of the AnNam Spa homepage using only HTML and CSS.

Open `index.html` directly in a browser. All images, logos, fonts and icons used by the page are stored locally under `assets/`.
